CREATE TABLE IF NOT EXISTS entreprises (
id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
est_active BOOLEAN,
nom VARCHAR(255),
adresse1 VARCHAR(255),
adresse2 VARCHAR(255),
code_postal VARCHAR(255),
ville VARCHAR(255),
CONSTRAINT pk_entreprises PRIMARY KEY (id)
);

